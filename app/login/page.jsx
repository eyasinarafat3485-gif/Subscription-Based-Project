import LoginClient from './LoginClient';

export const metadata = {
  title: 'Login',
  description: 'Login to your Developers Club account.',
};

export default function LoginPage() {
  return <LoginClient />;
}
